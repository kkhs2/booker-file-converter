# The aim is to be able to (in order of importance):
1. Convert all interface files to CSV format - mostly done
2. Ability to upload multiple files (same interface) in one go and for all data to be converted downloaded inside one single file - not started
3. Convert all interface files to JSON format - not started
4. Ability to update the interface specification - not started
5. Ability to generate (at least some) test data, like customers - not started

